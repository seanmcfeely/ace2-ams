import * as alerts from "./alerts";
import * as analysis from "./analysis";
import * as events from "./events";
import * as observables from "./observables";

export const configuration = { alerts, analysis, events, observables };
